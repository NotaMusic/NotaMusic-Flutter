import 'package:flutter/material.dart';

class StationButton extends StatelessWidget {
  const StationButton({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
